CREATE TABLE fighters(
                         id INT NOT NULL AUTO_INCREMENT,
                         name VARCHAR(40) NOT NULL ,
                         quote VARCHAR(255) DEFAULT 'Undisclosed',
                         PRIMARY KEY (id)
)